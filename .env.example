# ─── PRÉMAR — Variables d'environnement ───────────────────────
# Copier ce fichier en .env et remplir les valeurs

# PORT du serveur (Railway le définit automatiquement)
PORT=3000

# URL de connexion PostgreSQL (fournie automatiquement par Railway)
# Format : postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:OvbdKoWSSbYamtnYfNuLpYzoSilKBoYT@shinkansen.proxy.rlwy.net:42608/railway

# Environnement
NODE_ENV=production
# En production sur Railway : NODE_ENV=production
